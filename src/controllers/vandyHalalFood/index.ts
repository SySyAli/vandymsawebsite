import { Response, Request } from "express";

async function getHalalFood(req: Request, res: Response) {
  try {
    //
    
  } catch (error) {
    console.log(error);
    throw error;
  }
}

export {getHalalFood}